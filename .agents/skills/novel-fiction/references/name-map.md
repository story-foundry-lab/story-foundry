# 名称映射表

`scripts/check-names.py` 会读取本文件，检查旧名是否残留在作品 Markdown 中。

## 使用规则

- 只记录已经确定废弃、需要清理的旧名。
- 旧名可以用 `/` 分隔多个写法。
- 新名是当前正式写法。
- 允许在说明里标注“原名”“旧名”，检查脚本会跳过这类说明行。
- 不确定是否废弃的名字不要写进表里。

## 映射表

| 旧名 | 新名 | 范围 | 说明 |
|---|---|---|---|
